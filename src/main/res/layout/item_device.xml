<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp">


    <ImageView
        android:id="@+id/iv_veener"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:src="@mipmap/ic_launcher">

    </ImageView>

    <TextView
        android:id="@+id/tv_mac"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="12dp"
        style="@style/text_normal_black"
        app:layout_constraintLeft_toRightOf="@+id/iv_veener"
        app:layout_constraintTop_toTopOf="parent"
        android:text="192.9.168.255">

    </TextView>

    <ProgressBar
        android:id="@+id/progress_bar_h"
        style="?android:attr/progressBarStyleHorizontal"
        app:layout_constraintBottom_toBottomOf="@+id/iv_veener"
        app:layout_constraintLeft_toRightOf="@+id/iv_veener"
        android:layout_width="80dp"
        android:layout_height="20dp"
        android:layout_marginLeft="12dp"
        android:max="100"
        android:minHeight="20dp"
        android:maxHeight="20dp"
        android:indeterminate="false"
        android:progress="50" />


</androidx.constraintlayout.widget.ConstraintLayout>
